% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regimes.R
\name{define_regime}
\alias{define_regime}
\title{Define an Intervention Regime}
\usage{
define_regime(
  obj,
  name,
  static = NULL,
  dynamic = NULL,
  stochastic = NULL,
  description = NULL
)
}
\arguments{
\item{obj}{A \code{longy_data} object.}

\item{name}{Character. Name for this regime (must be unique).}

\item{static}{Integer (0 or 1). For static regimes: always treat (1) or never treat (0).}

\item{dynamic}{A function \code{f(data_row)} returning 0 or 1. For dynamic regimes.}

\item{stochastic}{A function \code{f(data_row)} returning P(A=1). For stochastic regimes.}

\item{description}{Character. Optional human-readable description.}
}
\value{
Modified \code{longy_data} object with the regime added.
}
\description{
Adds a treatment regime (static, dynamic, or stochastic) to a \code{longy_data} object.
}
\details{
Exactly one of \code{static}, \code{dynamic}, or \code{stochastic} must be provided.
}
\examples{
\dontrun{
obj <- obj |>
  define_regime(name = "always_treat", static = 1L) |>
  define_regime(name = "never_treat", static = 0L)
}

}
